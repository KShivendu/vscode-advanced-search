foo(007)
bar('zero zero seven')

// Search: `:[fn](:[numericArg~\d+])`
// Matches: Only `foo(007)`

// You can use the `~` operator to specify a regex to match the argument :D
// Note that this regex follows PCRE (Perl Compatible Regular Expressions) syntax.

